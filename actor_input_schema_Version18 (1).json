{
    "title": "Input schema of the Coworking Space Pricing scraper",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Start URLs",
            "type": "array",
            "description": "URLs to start crawling from (e.g. coworking site homepages or pricing pages).",
            "editor": "requestListSources",
            "prefill": [
                {
                    "url": "https://wework.com/locations"
                }
            ]
        },
        "maxRequestsPerCrawl": {
            "title": "Max Requests per Crawl",
            "type": "integer",
            "description": "Maximum number of requests that can be made by this crawler.",
            "default": 100
        }
    }
}